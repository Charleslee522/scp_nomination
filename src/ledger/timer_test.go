package ledger

import (
	"testing"
	// . "github.com/Charleslee522/scp_nomination/src/common"
)

func TestTimer(t *testing.T) {
	// consensus := NewConsensus("n1", 1, []Node{})
	// time.Sleep(1000 * time.Millisecond)
	// if consensus.Round != 4 {
	// 	t.Errorf("consensus.Round == %d, want %d", consensus.Round, 4)
	// }
	// if consensus.GetRoundDuration() != time.Duration(3)*Delay*time.Millisecond {
	// 	t.Errorf("consensus.GetRoundDuration() == %q, want %s", consensus.GetRoundDuration(), "900ms")
	// }
}

// func TestTimerQuit(t *testing.T) {
// 	consensus := NewConsensus("n1", 1, []Node{})
// 	time.Sleep(1000 * time.Millisecond)
// 	if consensus.GetRoundDuration() != time.Duration(3)*Delay*time.Millisecond {
// 		t.Errorf("consensus.GetRoundDuration() == %q, want %s", consensus.GetRoundDuration(), "900ms")
// 	}
// }
